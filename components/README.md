# Components Structure

## `/ui`
Componentes reutilizables de interfaz (botones, cards, inputs, etc.)

## `/layout`
Componentes de estructura (Header, Footer, Sidebar, etc.)

## `/tierlist`
Componentes específicos para el sistema de tierlist (TierRow, CompCard, etc.)

## `/admin`
Componentes del panel de administración
